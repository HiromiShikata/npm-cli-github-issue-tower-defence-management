name: Test

on:
  push:

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Install dependencies
        run: npm ci

      - name: Build native modules
        working-directory: node_modules/better-sqlite3
        run: node ../prebuild-install/bin.js || npm exec --yes -- node-gyp rebuild --release

      - name: Prepare
        run: npm run prepare

      - name: Build
        run: npm run build

      - name: Check credentials availability
        id: creds
        env:
          TEST_GH_TOKEN: ${{ secrets.TEST_GH_TOKEN }}
        run: |
          if [ -n "$TEST_GH_TOKEN" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::TEST_GH_TOKEN secret is unavailable. Integration tests are skipped. This is expected for dependabot/renovate PRs which do not have access to repository secrets."
          fi

      - name: Run tests
        if: steps.creds.outputs.available == 'true'
        run: npm run test
        env:
          GH_TOKEN: ${{ secrets.TEST_GH_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.TEST_GOOGLE_SERVICE_ACCOUNT_KEY }}
          SLACK_USER_TOKEN: ${{ secrets.TEST_SLACK_USER_TOKEN }}
          DOTENV_CONFIG_QUIET: true

      - name: Upload test results
        if: steps.creds.outputs.available == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: jest-junit-report
          path: reports/jest-junit

      - name: Upload artifacts
        if: steps.creds.outputs.available == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: reports
          path: reports
