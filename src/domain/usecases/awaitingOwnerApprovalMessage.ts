export const AWAITING_OWNER_APPROVAL_MESSAGE_HEAD =
  'Auto Status Check: AWAITING_OWNER_APPROVAL';

export const AWAITING_OWNER_APPROVAL_MESSAGE = `${AWAITING_OWNER_APPROVAL_MESSAGE_HEAD}
The last report declared that this task is waiting for owner approval. Moving it to Todo by human so that it waits in the owner's queue instead of being dispatched again.`;
